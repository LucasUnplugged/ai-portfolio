<!-- auto-queue -->
<!-- target-branch: feat/ledger-demo/remaining-screens -->

# Ledger Conflict Resolution Page

## Context
Stage 3 of Epic 2 (Ledger Demo). Build the Conflict Resolution screen at `/app/ledger/conflicts`.

Tech Spec: `.ai-epics/docs/ledger-demo/tech-spec.md`
Feature flag: `ledger_demo`

## Instructions

Create `src/app/app/ledger/conflicts/page.tsx` — a three-panel merge conflict resolution view.

**Design direction:** Git merge conflict resolution UI — three columns showing "mine", "merged result", and "theirs" with highlighted conflicts.

**Layout:**
```
┌──────────────────────────────────────────────────┐
│ ← Back to Dashboard       Conflict Resolution    │
│                                                    │
│ Conflict in "Technical Architecture"              │
│ Between Sarah Chen and Marcus Rivera              │
│ Section: Authentication Flow          Unresolved  │
│                                                    │
├────────────────┬────────────────┬────────────────┤
│ Sarah's Version│ Merged Result  │Marcus's Version│
├────────────────┼────────────────┼────────────────┤
│ version A text │ (resolution or │ version B text │
│ ...            │  placeholder)  │ ...            │
│                │                │                │
└────────────────┴────────────────┴────────────────┘
│                                                    │
│ [Keep Sarah's] [Manual Merge] [Keep Marcus's]     │
│                                                    │
└──────────────────────────────────────────────────┘
```

**Implementation:**

1. **Header:** Back link to Dashboard. Title "Conflict Resolution".

2. **Conflict info:** Document name, section title, conflicting users (with avatars), status badge (unresolved/resolved).

3. **Three-panel viewer:** Use the `ConflictPanel` component from `@/components/ledger/conflict-panel`. Pass the first conflict from mock data, resolved users.

4. **Resolution action buttons:** Three buttons below the panel:
   - "Keep Sarah's Version" (outline)
   - "Manual Merge" (primary)
   - "Keep Marcus's Version" (outline)
   All visual only — no click handlers.

5. **Data:** Use the first conflict from `conflicts` array. Import `conflicts`, `getConflictById`, `getUserById`, `getDocumentById` from `@/data/ledger`.

**Notes:**
- Server component
- The conflict panel shows 3 equal columns — on small screens, stack or use horizontal scroll
- Use `Badge` for status (unresolved=destructive, resolved=default)
- Use `Avatar` for user display

## Scope
- `src/app/app/ledger/conflicts/page.tsx` (new)

## Verification
- `npm run build` succeeds
- Conflict Resolution page renders with three-panel merge view

## Task Progress
<!-- lat: 2026-03-04T04:26:36Z -->
<!-- agent-pid: 24426 -->
<!-- worktree: .worktrees/q-003 -->
<!-- branch: q/003 -->

### Checklist
- [ ] **ACTIVE** → Create worktree and build Conflict Resolution page
- [ ] Verify build passes
- [ ] Merge and archive
