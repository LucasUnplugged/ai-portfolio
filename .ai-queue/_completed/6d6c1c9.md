<!-- auto-queue -->
<!-- target-branch: feat/ritual-demo/profile-matches-dm -->

# Ritual Matches Screen

Create the Matches screen at `src/app/app/ritual/matches/page.tsx`.

## Context

Tech spec: `.ai-epics/docs/ritual-demo/tech-spec.md`
Data: `src/data/ritual/` — use `matches`, `getUserById` from `@/data/ritual`
Components: `src/components/ritual/` — use `RitualShell`, `MatchCard`

Shows the user's connections — matches they've made and pending requests.

## Scope

- `src/app/app/ritual/matches/page.tsx` — new file

## Instructions

Build the Matches screen wrapped in `<RitualShell current="matches">`:

1. **Page title** — "Connections" in `font-heading text-xl`, with padding at top

2. **Tabbed view** using shadcn Tabs component:
   - Tab 1: "Matches" — shows matched connections
   - Tab 2: "Requests" — shows pending-received and pending-sent requests

3. **Matches tab content**:
   - Filter `matches` where `status === 'matched'`
   - Render each with `MatchCard` component
   - Get user data via `getUserById(match.userId)`
   - If no matches, show empty state: "No matches yet. Keep engaging with your circle!"

4. **Requests tab content**:
   - Filter `matches` where `status === 'pending-received'` or `status === 'pending-sent'`
   - Render each with `MatchCard` component
   - Pending-received shows Accept/Decline buttons (via MatchCard)
   - Pending-sent shows "Pending" badge
   - If no requests, show empty state

5. **Use the `line` variant** for TabsList: `<TabsList variant="line">` — this matches the tab style used in the Ledger demo and looks cleaner for mobile.

Layout: `p-4 space-y-4`.

This is a "use client" component.

## Verification

- Page renders with two tabs (Matches / Requests)
- Match cards display correctly with appropriate status indicators
- Bottom navigation shows "matches" tab active
- `npm run build` succeeds

## Task Progress
<!-- lat: 2026-03-04T04:56:09Z -->
<!-- agent-pid: 24426 -->
<!-- worktree: .worktrees/q-002 -->
<!-- branch: q/002 -->

### Checklist
- [ ] **ACTIVE** → Build matches page with tabs
- [ ] Verify build passes
