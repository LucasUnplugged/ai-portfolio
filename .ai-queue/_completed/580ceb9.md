<!-- target-branch: feat/foundation-landing-case-studies/shadcn-theming-setup -->

# Commit History
- `2d103b7` feat(case-studies): add placeholder pages for ledger, ritual, circles
- `580ceb9` merge: segment 002 — case study placeholder pages

# Segment 2: Case Study Placeholder Pages

## Objective
Create the 3 case study route directories with simple placeholder pages. These pages will be fully built out in Stage 4 — for now they just need to exist at the correct routes with minimal content.

## Scope
- `src/app/case-studies/ledger/page.tsx` (new)
- `src/app/case-studies/ritual/page.tsx` (new)
- `src/app/case-studies/circles/page.tsx` (new)

## Instructions

### Step 1: Create case study directories

```bash
mkdir -p src/app/case-studies/ledger
mkdir -p src/app/case-studies/ritual
mkdir -p src/app/case-studies/circles
```

### Step 2: Create placeholder pages

Each page should be a simple server component with minimal content — just enough to confirm the route works. Do NOT import any shadcn components (to keep this segment dependency-free). Use basic HTML + Tailwind classes only.

**`src/app/case-studies/ledger/page.tsx`:**
```tsx
export default function LedgerCaseStudy() {
  return (
    <main className="min-h-screen flex items-center justify-center p-8">
      <div className="text-center space-y-4">
        <h1 className="text-4xl font-bold tracking-tight">Ledger</h1>
        <p className="text-lg text-muted-foreground">
          Multiplayer project management &amp; knowledge base
        </p>
        <p className="text-sm text-muted-foreground">Case study coming soon</p>
      </div>
    </main>
  );
}
```

**`src/app/case-studies/ritual/page.tsx`:**
```tsx
export default function RitualCaseStudy() {
  return (
    <main className="min-h-screen flex items-center justify-center p-8">
      <div className="text-center space-y-4">
        <h1 className="text-4xl font-bold tracking-tight">Ritual</h1>
        <p className="text-lg text-muted-foreground">
          Structured, time-bound social connections
        </p>
        <p className="text-sm text-muted-foreground">Case study coming soon</p>
      </div>
    </main>
  );
}
```

**`src/app/case-studies/circles/page.tsx`:**
```tsx
export default function CirclesCaseStudy() {
  return (
    <main className="min-h-screen flex items-center justify-center p-8">
      <div className="text-center space-y-4">
        <h1 className="text-4xl font-bold tracking-tight">Circles</h1>
        <p className="text-lg text-muted-foreground">
          Relationship maintenance &amp; personal CRM
        </p>
        <p className="text-sm text-muted-foreground">Case study coming soon</p>
      </div>
    </main>
  );
}
```

### Step 3: Verify build

```bash
npm run build
```

All 3 routes should build successfully:
- `/case-studies/ledger`
- `/case-studies/ritual`
- `/case-studies/circles`

## Verification
- All 3 case study pages exist at correct paths
- Each renders a heading with the project name and one-line description
- `npm run build` succeeds
- Routes `/case-studies/ledger`, `/case-studies/ritual`, `/case-studies/circles` are all built

## Task Progress
<!-- lat: 2026-03-04T03:17:45Z -->
<!-- agent-pid: 44000 -->
<!-- worktree: .worktrees/q-002 -->
<!-- branch: q/002 -->

### Checklist
- [x] Create case study directories
- [x] Create placeholder pages (ledger, ritual, circles)
- [x] Verify build passes
- [ ] **ACTIVE** → Merge and cleanup
- [ ] Merge and cleanup

### Handoff Context
- Target branch: feat/foundation-landing-case-studies/shadcn-theming-setup
- Simple placeholder pages with basic HTML + Tailwind only (no shadcn imports)
