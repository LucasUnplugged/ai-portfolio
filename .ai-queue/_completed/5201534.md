# Commit History
- 5201534: Merge q/003: integrate DemoHeader and PhoneSimulator into demo layouts
- 56549cf: feat(demo): integrate DemoHeader and PhoneSimulator into demo layouts

<!-- auto-queue -->
<!-- depends-on: 002 -->
<!-- target-branch: feat/foundation-landing-case-studies/shared-components-demo-layouts -->

# Segment 3: Update Demo Layouts & Placeholder Pages

## Objective
Update all 3 demo layouts to integrate DemoHeader and PhoneSimulator (where applicable), and refresh the demo placeholder pages to verify theming.

## Scope
- `src/app/app/ledger/layout.tsx` (modify)
- `src/app/app/ledger/page.tsx` (modify)
- `src/app/app/ritual/layout.tsx` (modify)
- `src/app/app/ritual/page.tsx` (modify)
- `src/app/app/circles/layout.tsx` (modify)
- `src/app/app/circles/page.tsx` (modify)

## Instructions

### Step 1: Update Ledger layout

Modify `src/app/app/ledger/layout.tsx`:
- Import and add `DemoHeader` with `current="ledger"`
- Ledger is desktop-only — do NOT wrap in PhoneSimulator
- Keep the existing theme wrapper (`ledger-theme` class, `bg-background text-foreground`)

```tsx
import type { ReactNode } from "react";
import { DemoHeader } from "@/components/demo-header";
import "./globals.css";

export default function LedgerLayout({ children }: { children: ReactNode }) {
  return (
    <div className="ledger-theme min-h-screen bg-background text-foreground">
      <DemoHeader current="ledger" />
      {children}
    </div>
  );
}
```

### Step 2: Update Ritual layout

Modify `src/app/app/ritual/layout.tsx`:
- Import `DemoHeader` with `current="ritual"`
- Import `PhoneSimulator` and wrap `{children}` inside it
- Ritual uses a dark theme — the `ritual-theme` class handles this
- DemoHeader sits OUTSIDE the phone simulator (at the top of the page)
- PhoneSimulator contains only the page content

```tsx
import type { ReactNode } from "react";
import { DemoHeader } from "@/components/demo-header";
import { PhoneSimulator } from "@/components/phone-simulator";
import "./globals.css";

export default function RitualLayout({ children }: { children: ReactNode }) {
  return (
    <div className="ritual-theme min-h-screen bg-background text-foreground">
      <DemoHeader current="ritual" />
      <PhoneSimulator>
        {children}
      </PhoneSimulator>
    </div>
  );
}
```

### Step 3: Update Circles layout

Modify `src/app/app/circles/layout.tsx`:
- Same pattern as Ritual but with `current="circles"` and `circles-theme`

```tsx
import type { ReactNode } from "react";
import { DemoHeader } from "@/components/demo-header";
import { PhoneSimulator } from "@/components/phone-simulator";
import "./globals.css";

export default function CirclesLayout({ children }: { children: ReactNode }) {
  return (
    <div className="circles-theme min-h-screen bg-background text-foreground">
      <DemoHeader current="circles" />
      <PhoneSimulator>
        {children}
      </PhoneSimulator>
    </div>
  );
}
```

### Step 4: Update Ledger placeholder page

Modify `src/app/app/ledger/page.tsx`:
- Keep the existing Card-based layout but make it more appropriate for a desktop demo
- The page should fill available space and demonstrate the amber theme

```tsx
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";

export default function LedgerDemo() {
  return (
    <div className="flex flex-col items-center justify-center min-h-[calc(100vh-3rem)] p-8">
      <Card className="w-full max-w-lg">
        <CardHeader>
          <div className="flex items-center gap-2">
            <CardTitle className="text-2xl">Ledger</CardTitle>
            <Badge variant="secondary">Desktop</Badge>
          </div>
        </CardHeader>
        <CardContent className="space-y-4">
          <p className="text-muted-foreground">
            Multiplayer project management &amp; knowledge base. Desktop-first
            layout with warm amber theme.
          </p>
          <div className="flex gap-2">
            <Button>Explore Demo</Button>
            <Button variant="outline">View Case Study</Button>
          </div>
        </CardContent>
      </Card>
    </div>
  );
}
```

### Step 5: Update Ritual placeholder page

Modify `src/app/app/ritual/page.tsx`:
- Simplified mobile-friendly content (renders inside PhoneSimulator)
- Dark theme — use appropriate contrast

```tsx
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";

export default function RitualDemo() {
  return (
    <div className="flex flex-col items-center justify-center min-h-full p-6">
      <Card className="w-full max-w-sm">
        <CardHeader>
          <div className="flex items-center gap-2">
            <CardTitle className="text-xl">Ritual</CardTitle>
            <Badge variant="secondary">Mobile</Badge>
          </div>
        </CardHeader>
        <CardContent className="space-y-3">
          <p className="text-sm text-muted-foreground">
            Structured, time-bound social connections. Dark purple theme
            with serif headings.
          </p>
          <Button className="w-full">Start a Ritual</Button>
        </CardContent>
      </Card>
    </div>
  );
}
```

### Step 6: Update Circles placeholder page

Modify `src/app/app/circles/page.tsx`:
- Mobile-friendly content (renders inside PhoneSimulator)
- Light teal theme

```tsx
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";

export default function CirclesDemo() {
  return (
    <div className="flex flex-col items-center justify-center min-h-full p-6">
      <Card className="w-full max-w-sm">
        <CardHeader>
          <div className="flex items-center gap-2">
            <CardTitle className="text-xl">Circles</CardTitle>
            <Badge variant="secondary">Mobile</Badge>
          </div>
        </CardHeader>
        <CardContent className="space-y-3">
          <p className="text-sm text-muted-foreground">
            Relationship maintenance &amp; personal CRM. Clean teal
            theme with rounded elements.
          </p>
          <Button className="w-full">View Circles</Button>
        </CardContent>
      </Card>
    </div>
  );
}
```

### Step 7: Verify build

```bash
npm run build
```

All 3 demo routes should build successfully with correct theme application.

## Verification
- Ledger layout has DemoHeader but NO PhoneSimulator
- Ritual layout has DemoHeader AND PhoneSimulator
- Circles layout has DemoHeader AND PhoneSimulator
- All 3 placeholder pages render with themed content
- `npm run build` succeeds

## Task Progress
<!-- lat: 2026-03-03T22:42:00Z -->
<!-- agent-pid: $PPID -->
<!-- worktree: .worktrees/q-003 -->
<!-- branch: q/003 -->

### Checklist
- [ ] **ACTIVE** → Update Ledger layout + page
- [ ] Update Ritual layout + page
- [ ] Update Circles layout + page
- [ ] Verify build passes

### Handoff Context
- Ledger: DemoHeader only (desktop), no PhoneSimulator
- Ritual/Circles: DemoHeader + PhoneSimulator
- All layouts import their globals.css for theme
